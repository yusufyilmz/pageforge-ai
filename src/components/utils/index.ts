// Page utilities for metadata and schema generation
export {
  PageMeta,
  generatePageMetadata,
  extractMetadataFromPageConfig,
  type PageMetaProps,
} from "./page-metadata";

export {
  PageSchema,
  StructuredData,
  extractStructuredDataFromPageConfig,
  type StructuredDataProps,
} from "./page-schema";
